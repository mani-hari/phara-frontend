// Default for the implicit `children` slot at the /account index (there is no
// account/page.tsx — the index is served by the @dashboard/@login slots).
export default function Default() {
  return null
}
